@model Albatros.Models.ApplicationUser
@{
    ViewData["Title"] = "Delete User";
}

<div class="admin-card" style="max-width:550px; margin:40px auto; text-align:center;">
    <div style="font-size:48px; margin-bottom:16px;">👤</div>
    <h1 style="font-size:24px; color:#C62828; margin-bottom:12px;">Delete User Account</h1>
    <p style="color:#8A8580; margin-bottom:24px; line-height:1.6;">
        Are you sure you want to permanently delete user <strong style="color:#0A0A0A;">#@Model.UserId (@Model.FullName)</strong> and all associated listings/reviews?
    </p>

    <form asp-area="Admin" asp-controller="Users" asp-action="Delete" asp-route-id="@Model.UserId" method="post">
        @Html.AntiForgeryToken()
        <div style="display:flex; gap:12px; justify-content:center;">
            <button type="submit" class="btn" style="background:#D32F2F; color:white; padding:10px 24px;">Confirm Deletion</button>
            <a asp-area="Admin" asp-controller="Users" asp-action="Index" class="btn btn-ghost" style="padding:10px 24px;">Cancel</a>
        </div>
    </form>
</div>
