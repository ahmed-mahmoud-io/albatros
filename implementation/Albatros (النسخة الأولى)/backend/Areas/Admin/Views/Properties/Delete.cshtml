@model Albatros.Models.Property
@{ ViewData["Title"] = "Delete Property"; }

<h2 style="font-family:'Playfair Display',serif;">Delete Property</h2>
<p>Are you sure you want to delete this listing?</p>

<div class="card mb-3" style="max-width:480px;">
    <div class="card-body">
        <h5 class="card-title">@Model.Title</h5>
        <p class="card-text text-muted">Owner: @Model.User?.FullName</p>
    </div>
</div>

<form asp-area="Admin" asp-controller="Properties" asp-action="Delete" asp-route-id="@Model.PropertyId" method="post">
    @Html.AntiForgeryToken()
    <button type="submit" class="btn btn-danger">Yes, Delete</button>
    <a href="/Admin/Properties" class="btn btn-outline-secondary">Cancel</a>
</form>
